---
import Slide from '@decks/templates/Slide.astro';

import Placeholder from './_Placeholder.astro';
---

<Slide subtitle="Nested items">
	<Placeholder need="Nesting with the drag handle still present" why="Two problems with the current bench. The twisty on a parent sits exactly where the drag handle sits, and this bench never shows the handle, so the obvious question is what happened to it. And the drop indicator refuses some positions for reasons I have not traced yet. Both need fixing before this is presentable." height={470} />
</Slide>
