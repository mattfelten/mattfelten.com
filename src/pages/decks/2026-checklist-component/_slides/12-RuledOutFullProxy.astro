---
import Slide from '@decks/templates/Slide.astro';

import Placeholder from './_Placeholder.astro';
---

<Slide subtitle="Reordering → Ruled out: the whole row follows the cursor">
	<Placeholder need="The full-height proxy, carrying the person badge" why="A full height block covers the list you are aiming at. The current proxy drops the person badge that every row shows, so the comparison is not like for like. Needs the proxy to render the badge." height={470} />
</Slide>
