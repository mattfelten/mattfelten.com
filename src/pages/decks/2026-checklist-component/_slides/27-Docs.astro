---
import Slide from '@decks/templates/Slide.astro';

import Placeholder from './_Placeholder.astro';
---

<Slide subtitle="Docs">
	<Placeholder
		need="The component docs, for people and for agents"
		why="Purpose, anatomy, states, interactions, accessibility notes. And the machine-readable half, which half exists already: every switch has a name, unknown values are ignored rather than guessed, and every decision has a URL that opens the component in exactly that state."
		height={440}
	/>
</Slide>
