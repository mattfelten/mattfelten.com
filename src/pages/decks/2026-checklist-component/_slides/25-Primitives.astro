---
import Slide from '@decks/templates/Slide.astro';

import Placeholder from './_Placeholder.astro';
---

<Slide subtitle="The system → What it would own">
	<Placeholder
		need="A diagram of the four primitives, assembled"
		why="ListRow, EditableText, SortableList, PresenceMarker. Four names in a grid does not explain what any of them is for. This needs to show one row exploded into its parts, so the purpose of each is obvious and the question of how they compose answers itself."
		height={520}
	/>
</Slide>
