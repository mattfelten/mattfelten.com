---
import Slide from '@decks/templates/Slide.astro';

import Placeholder from './_Placeholder.astro';
---

<Slide subtitle="Nested items → Ruled out: nesting without a limit">
	<Placeholder need="A list nested three or four levels deep" why="Replaces the sideways-drag rejection, which the bench does not actually demonstrate. Default state should be a parent with a child, with a child, with a child, so the indent eats the row and the space problem is visible rather than argued." height={470} />
</Slide>
