---
const { title, href, class: className } = Astro.props;
---

<section class={className}>
	<div class="mb-1 flex items-baseline justify-between">
		<h3 class="text-subdued font-medium">{title}</h3>
		{href && (
			<a href={href} class="text-subdued hover:text-accent text-sm font-medium transition-colors">
				View all →
			</a>
		)}
	</div>
	<slot />
</section>
