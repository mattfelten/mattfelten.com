---
const { columns = 3 } = Astro.props;
---

<div
	class:list={[
		'not-prose not-fullwidth my-8 grid gap-4',
		columns === 2 && 'grid-cols-2',
		columns === 3 && 'grid-cols-2 sm:grid-cols-3',
		columns === 4 && 'grid-cols-2 sm:grid-cols-4',
	]}
>
	<slot />
</div>
