---
title: Atlas
company: Mission Cloud
year: 2024
active: true
type: case-study
featured: true
parent: mission-control
intro: An internal tools platform I pitched, designed, and built on spare cycles while the team focused on other priorities. It's now where over a third of the engineering team's roadmap lives.
cover: ./imgs/dashboard.png
coverAlt: The Atlas dashboard showing a personalized overview of customer activity, assigned work, and recent updates
---

import Asset from '../../../components/Asset.astro';
import AssetRow from '../../../components/AssetRow.astro';
import Card from '../../../components/Card.astro';
import PullQuote from '../../../components/PullQuote.astro';
import Stat from '../../../components/Stat.astro';
import Stats from '../../../components/Stats.astro';

import awsAccountOverview from './imgs/aws-account-overview.png';
import billingAdjustment from './imgs/billing-adjustment.png';
import billingRules from './imgs/billing-rules.png';
import contractUpload from './imgs/contract-upload.png';
import customerOverview from './imgs/customer-overview.png';
import loopTimeline from './imgs/loop-timeline.png';
import mvpCustomerOverview from './imgs/mvp-customer-overview.png';
import mvpCustomerTeam from './imgs/mvp-customer-team.png';
import mvpCustomers from './imgs/mvp-customers.png';
import pricingDimensions from './imgs/pricing-dimensions.png';

[Mission Cloud's](https://missioncloud.com/) customer-facing app, Mission Control, kept bumping into a gap. The Product Development team I was on ran into projects that weren't straightforward customer features. Cross-customer reports. Internal workflows. Data customers shouldn't see. Our legacy internal tool was a decade out of date, slow, and unmaintained. So I spent two months of spare cycles designing and building Atlas, an internal tools platform that now accounts for over a third of the engineering team's roadmap.

---

## The Problem

<PullQuote title="Problem Statement">
Supporting Mission's customers takes more than a customer-facing app. Internal teams need their own **data, workflows, and tools** to do their work.

Today, that work is scattered across a **customer-facing app where it doesn't belong**, a **legacy tool that's been left to rot**, and one-off scripts and spreadsheets.

Without a **dedicated home for internal tools**, teams lose time, take on **security risk**, and avoid building the features they actually need.

</PullQuote>

<div class="not-prose my-8 grid gap-4 sm:grid-cols-3">
	<Card title="Features without a home">
		Internal reports, operational data, tools for managing third-party
		integrations. None of it belonged in the customer-facing app, but there
		was nowhere else to put it.
	</Card>
	<Card title="A legacy tool on life support">
		Queries that took 10 minutes. Security issues nobody wanted to touch. A
		codebase disconnected from everything else we'd built.
	</Card>
	<Card title="Screen-sharing risk">
		Mission's teams share their screens with customers constantly. Internal
		data in the customer app was a wrong-click away from being visible
		during a live call.
	</Card>
</div>

---

## The Pitch

To make the idea tangible, I designed conceptual screens showing where internal data would live and how it could scale. I framed it as an MVP. One useful view, prove the architecture, add from there.

Leadership liked the vision. But they heard "second app" and thought about the many months it took to stand up Mission Control. The auth layer, the APIs, the build tooling, the CI. Engineering was already stretched thin, let alone on a speculative project. They loved the idea but couldn't prioritize it.

The concern was understandable. It was also solvable. Those were exactly the problems a monorepo architecture was designed to eliminate. I expanded my pitch to explain that Atlas was a second app, but not a second codebase. A monorepo would let both apps share authentication, API access, and UI components. I made the case that I could do the infrastructure work on spare cycles without pulling anyone off the existing roadmap. They gave me the green light.

## Building It

Two months of spare cycles, December 2023 to February 2024. I phased the work into small, reviewable chunks where each PR improved the existing codebase on its own. Even if Atlas never shipped, the work would pay for itself. The MVP was a searchable list of customers, built on existing API endpoints. That was it.

<div class="not-prose my-8 grid gap-4 sm:grid-cols-2">
	<Card title="Monorepo migration">
		Converted the single Mission Control repo into a monorepo so both apps
		could share code and dependencies while being deployed independently.
	</Card>
	<Card title="Webpack to Vite">
		Swapped the build tool to Vite. Easier to configure for a multi-app
		setup, faster builds across the board.
	</Card>
	<Card title="Shared design system">
		Extracted UI components into a shared library that both apps consume.
		Same components, same visual language, one source of truth.
	</Card>
	<Card title="Auth and API refactor">
		Decoupled the authentication layer and GraphQL connections from Mission
		Control so both apps could authenticate and query independently.
	</Card>
</div>

<AssetRow>
	<Asset
		src={mvpCustomers}
		alt="The MVP Customers list view, a simple searchable directory of every customer"
		expand
	/>
	<Asset
		src={mvpCustomerOverview}
		alt="The MVP customer overview page for an example customer, showing basic account information"
		expand
	/>
	<Asset
		src={mvpCustomerTeam}
		alt="The MVP customer team page showing the Mission staff assigned to that customer"
		expand
	/>
</AssetRow>

---

## Growth

We didn't announce Atlas to the broader company for about a year. Instead, we shared it quietly with individuals who'd find it useful. An Account Manager looking up customer details. An engineer who wanted AWS account information without waiting 10 minutes for the legacy tool. They used it more than expected, shared it with more coworkers, and started requested features. Each new feature was a side project that I designed ahead of time so the team could ship whenever the next spare cycle landed. The backlog kept growing until Atlas had to be actively planned in quarterly roadmaps.

About a year and a half in, we'd reached near feature parity with the legacy tool. Combined with that legacy tool's mounting security issues, we made the call to port over the remaining functionality and sunset it entirely.

---

## What It Is Today

Atlas has grown into six primary areas, each cross-linked to the others and to Mission Control.

<div class="not-prose my-8 grid gap-4 sm:grid-cols-2">
	<Card title="Dashboard">
		A personalized starting point surfacing data relevant to the logged-in
		user.
	</Card>
	<Card title="Customers">
		Every customer, drillable into products, AWS accounts, integrations, and
		contracts.
	</Card>
	<Card title="AWS Accounts">
		Every AWS account across all customers, with support type, access, org
		structure, and sync state.
	</Card>
	<Card title="Entitlements">
		A full entitlements system with audit tracking for what each customer
		can access.
	</Card>
	<Card title="Billing and Finance">
		Workflow tools for finance teams. Contracts, billing rules, adjustments,
		and reporting.
	</Card>
	<Card title="Loops">
		All Collaboration Loops across all customers in a single internal view.
	</Card>
</div>

---

## Designing for multiple mental models

Atlas serves several teams at Mission, each approaching the same data with a different mental model. Account Managers start with a customer. Engineers start with an AWS account. Finance starts with a customer to find a contract. Support starts with whichever piece of information the case gives them. The naive IA would pick one of those mental models and force everyone through it. Atlas does both.

So every data object has two homes. A top-level list view for when you don't yet know which one you're looking for. A contextual placement inside a related object's detail page for when you do. AWS accounts live in a top-level AWS Accounts list and inside each customer's detail page. Loops live in a top-level view and inside each customer. Contracts, users, integrations all follow the same pattern.

We kept the maintenance cost manageable with one strict rule. Actions only live on detail pages. The top-level AWS Accounts list and the customer-scoped list both render as read-only directories. Click any row from either path and you arrive at the same AWS Account Details page. One canonical detail view per object, regardless of how you got there.

---

## The Product

<Asset
	src={customerOverview}
	expand
	caption="A customer's overview page. AWS accounts, contracts, integrations, and Loops all show up in context for teams that start with a customer in mind."
/>

<Asset
	src={awsAccountOverview}
	expand
	caption="An AWS account overview. Same data, accessed account-first. The customer is preserved as a link, not duplicated as a view."
/>

<Asset
	src={loopTimeline}
	expand
	caption="The Loop Timeline view inside a customer. Every meeting and its state, without bouncing between tools."
/>

Finance needed more than surfaced data. They needed workflow tools. Uploading contracts, applying billing adjustments, configuring tiered support fee rules with the SQL that drives the billing engine. These are the screens finance teams stare at every month. They deserved the same care as any customer-facing surface.

<AssetRow>
	<Asset
		src={contractUpload}
		expand
		caption="The Contract Upload modal. A structured form for what used to be tracked in spreadsheets."
	/>
	<Asset
		src={pricingDimensions}
		expand
		caption="Pricing dimensions, the building blocks for billing rules and adjustments."
	/>
</AssetRow>

<AssetRow>
	<Asset
		src={billingAdjustment}
		expand
		caption="The Billing Adjustment modal. A guided flow for corrections that used to require an engineer."
	/>
	<Asset
		src={billingRules}
		expand
		caption="The Billing Rules modal. Finance can read and verify the underlying SQL without an engineer translating it."
	/>
</AssetRow>

---

## What I Learned

### Infrastructure as product design

The monorepo, shared design system, and build tool swap were the hardest things to get buy-in for. They sounded like engineering overhead. They were actually the product decision. Every feature after the MVP was cheap because the foundation was shared. The auth refactor meant no new login flow. The shared components meant no new design system. The monorepo meant no new deploy pipeline. The infrastructure wasn't a prerequisite for Atlas. It was the whole project, why we could do it in the first place. The customer list was just proof that it worked.

---

## Outcomes

<Stats columns={3}>
	<Stat value="34k+" label="monthly views across Atlas's primary surfaces" />
	<Stat value="70%" label="of Mission employees have used Atlas" />
	<Stat value="35%" label="of engineering roadmap is Atlas projects" />
</Stats>

Atlas is the daily-driver for Mission's customer-facing teams. Account Managers, Customer Success, Support Engineers, Onboarding, Finance, Revenue Ops, and the engineers keeping it all running. The AWS Accounts list alone saw 18,400 views in March.

The roadmap share isn't scope creep. The company kept finding problems where the right place to solve them was inside Atlas. The legacy tool has been fully sunset, and every new internal feature now costs a fraction of what it would have with a separate codebase.
